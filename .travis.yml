matrix:
  include:
    - language: java
      install: true
      jdk:
        - openjdk11
      script:
        - ./gradlew build

    - language: ruby
      services:
        - docker
      before_install:
        - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
      script:
        - ./build-docker-image-api.sh

    - language: ruby
      services:
        - docker
      before_install:
        - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
      script:
        - ./build-docker-image-customer.sh

    - language: ruby
      services:
        - docker
      before_install:
        - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
      script:
        - ./build-docker-image-inventory.sh

    - language: ruby
      services:
        - docker
      before_install:
        - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
      script:
        - ./build-docker-image-payment.sh
